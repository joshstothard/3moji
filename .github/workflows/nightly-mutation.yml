name: Nightly Mutation Tests

on:
  # Schedule disabled per ADR-0002 to conserve Actions minutes on a private repo;
  # run manually from the Actions tab or re-enable the cron.
  # schedule:
  #   - cron: "0 2 * * 1-5"
  workflow_dispatch:

jobs:
  mutation:
    name: Stryker mutation testing
    runs-on: ubuntu-latest
    permissions:
      contents: read

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: app
          POSTGRES_PASSWORD: app
          POSTGRES_DB: app_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/setup

      - name: Run Stryker
        run: npx stryker run
        env:
          DATABASE_URL: postgresql://app:app@localhost:5432/app_test
        continue-on-error: true

      - name: Upload mutation report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: stryker-report
          path: reports/mutation/
          retention-days: 30
