node_modules/
dist/
build/
.next/
.turbo/
coverage/
reports/
!docs/reports/
*.log
*.tsbuildinfo

# Environment — never commit credentials
.env
.env.*
!.env.example

# Private, local-only docs — machine-local junction, never publish
.private/

# OS noise
.DS_Store
Thumbs.db

# Playwright test output — traces, screenshots and the HTML reporter
test-results/
playwright-report/

# Playwright MCP output — screenshots saved to repo root by /run skill
.playwright-mcp/
/*.png
/*.jpg
/*.jpeg
