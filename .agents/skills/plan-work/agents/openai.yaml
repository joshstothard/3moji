interface:
  display_name: "Plan Work"
  short_description: "Turn a workstream phase into an epic with sub-issues"
  default_prompt: "Use $plan-work with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
