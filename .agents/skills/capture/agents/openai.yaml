interface:
  display_name: "Capture"
  short_description: "Capture this conversation as a GitHub issue"
  default_prompt: "Use $capture with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
