interface:
  display_name: "Briefing"
  short_description: "Build read-only GitHub issue and pull request context"
  default_prompt: "Use $briefing with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
