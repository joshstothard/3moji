interface:
  display_name: "ADR"
  short_description: "Draft a numbered ADR and sync the architecture docs"
  default_prompt: "Use $adr with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
