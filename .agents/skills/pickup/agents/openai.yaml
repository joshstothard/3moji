interface:
  display_name: "Pickup"
  short_description: "Prepare a GitHub issue for implementation"
  default_prompt: "Use $pickup with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
