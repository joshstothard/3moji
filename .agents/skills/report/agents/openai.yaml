interface:
  display_name: "Report"
  short_description: "Write a dated research, spike, or retro report"
  default_prompt: "Use $report with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
