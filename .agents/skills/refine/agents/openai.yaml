interface:
  display_name: "Refine"
  short_description: "Refine a GitHub issue before implementation"
  default_prompt: "Use $refine with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
