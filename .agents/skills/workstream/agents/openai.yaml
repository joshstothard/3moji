interface:
  display_name: "Workstream"
  short_description: "Scope a body of work into phases with acceptance criteria"
  default_prompt: "Use $workstream with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
